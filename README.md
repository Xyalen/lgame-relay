# L-Game Relay Server

WebSocket relay server for L-Game Online.
Forwards messages between two players in a room.

## Setup
Install dependencies:
pip install websockets

## Run locally
python relay_server.py
